#include<stdio.h>
#include<conio.h>
main()
{
int n,i,r,sum=0;
printf("\n enter the number: ");
scanf("%d",&n);
while(n!=0)
{
r=n%10;
n=n/10;
count++
}
printf("\n count is:%d",count);
getch();
return 0;
}
